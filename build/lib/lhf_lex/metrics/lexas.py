from __future__ import annotations
from typing import Dict, Iterable

def compute(texts: Iterable[str], *, config: dict | None = None) -> Dict[str, float]:
    return {"LAS": float("nan"), "notes": 0.0}
